# requirements.txt
# Python dependencies for macOS development environment
# Install with: pip install -r requirements.txt

# PyTorch for macOS (MPS/CPU support)
# For macOS with Apple Silicon, use: pip install torch torchvision torchaudio
torch>=2.5.0

# Core ML dependencies
numpy>=1.24.4
tiktoken>=0.7.0
pandas>=2.0.0

# Dataset handling
datasets>=2.19.0
pyarrow>=14.0.2
tqdm>=4.66.1

# Experiment tracking
wandb>=0.17.0

# HuggingFace transformers (for GPT-2 pretrained model loading)
transformers>=4.44.0

# Evaluation
lm-eval>=0.4.0
